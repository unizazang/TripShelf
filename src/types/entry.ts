export type TripEntry = {
  id: string;
  tripId: string;
  entryDate: string;
  title: string;
  content: string;
  imageUrl: string | null;
  moodTag: string | null;
  createdAt: string;
  updatedAt: string;
};